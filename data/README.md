# Data Directory Reference

This directory contains all input definitions and pipeline output for Factum Atlas.
Runtime artifacts (`vault2/`, `raw_ingest/`, `atlas/`, `progress.json`) are gitignored — they are generated by the pipeline.

---

## Tracked (in git)

### `data/events/{id}.json`
One file per event. The pipeline ingests articles and extracts predictions for each event.

```json
{
  "id": "C05",
  "name": "Iran launches direct missile attack on Israel",
  "outcome": true,
  "outcome_date": "2024-04-14",
  "search_keywords": ["מתקפה איראנית ישירה", "Iran missile attack Israel"],
  "llm_referee_criteria": "Must focus on the April 14 multi-drone/missile barrage."
}
```

| Field | Required | Description |
|---|---|---|
| `id` | ✓ | Unique event ID, letter prefix + zero-padded index (`A01`). Must match the filename. |
| `name` | ✓ | Human-readable event description |
| `outcome` | ✓ | `true` = event happened, `false` = did not happen, `null` = unresolved |
| `outcome_date` | if resolved | Date the event resolved (ISO 8601). Required when `outcome` is not null. |
| `search_keywords` | ✓ | List of keywords for GNews/DDG/CDX search. Hebrew + English. |
| `llm_referee_criteria` | ✓ | Prompt fragment given to the LLM to judge relevance |
| `category` | ✓ | List of display categories (`Israeli Politics`, `Gaza War`, `Energy`, …) |
| `tags` | ✓ | Free-form string tags |
| `polymarket` | ✓ (nullable) | `null`, or `{question, url, match_quality}` with optional `invert`. `match_quality` ∈ `exact` \| `close` \| `indirect`. The key is always present — an explicit `null` means "no market", a missing key is a malformed file. |
| `predictive_window_days` | optional | How many days before `outcome_date` to search. **The fallback differs by ingest path** — 14 in `gnews_ingest`/`site_search`/`crawler`/`orchestrator`, 30 in `gdelt_ingest`/`web_search_ingest` — so every committed event sets it explicitly (all currently 30). |
| `oracle_question` | optional | Binary question text when the event is also posed to the Oracul |
| `duel_keywords` | optional | Extra keywords used by the Polymarket duel |
| `scope` | optional | e.g. `global` |

**Event ID prefixes:**
- `A` — Israeli politics / elections
- `B` — Gaza / Hamas conflict
- `C` — Iran / regional escalation
- `D` — Israeli economy
- `E` — Global events (Trump, Fed, etc.)
- `F` — Society / religion
- `G` — Tech / AI
- `H` — Israeli tech / economy
- `I` — Energy / global markets

The prefix is a curation bucket and does **not** have to match `category` — an `A` event can carry `Gaza War`. New letters are expected as the atlas grows.

**Validating these files** — two scripts, run from the repo root:

```bash
python pipeline/scripts/validate_events.py       # schema: fields, types, ids, dates
python pipeline/scripts/audit_event_criteria.py  # quality of llm_referee_criteria
```

`validate_events.py` exits non-zero on any finding and is pinned by
`pipeline/tests/test_event_validation.py`, which asserts the committed set is
clean — so a bad hand edit fails CI rather than silently producing a starved or
mis-keyed case study. Editing anything under `data/events/` triggers that
workflow.

---

### `data/sources/{id}.json`
One file per news source. Controls which outlets the pipeline ingests.

```json
{
  "id": "toi",
  "name": "Times of Israel",
  "url": "https://www.timesofisrael.com",
  "country": "IL",
  "language": "en",
  "type": "newspaper",
  "api_provider": null,
  "ingestion_status": "pending",
  "political_lean": null,
  "economic_lean": null,
  "notes": "English-language Israeli outlet"
}
```

| Field | Description |
|---|---|
| `id` | Unique source ID (used as directory name in raw_ingest/) |
| `url` | Base URL — domain extracted for CDX/GNews search |
| `language` | ISO 639-1 code. `he` sources use Hebrew GNews locale |
| `ingestion_status` | `pending` / `active` / `disabled` |

---

## Generated at runtime (gitignored)

### `data/progress.json`
Tracks pipeline state for every (event, source) cell.

```json
{
  "cells": {
    "C05:toi": {
      "event_id": "C05",
      "source_id": "toi",
      "status": "done",
      "prediction_count": 4,
      "error": null
    }
  }
}
```

**Cell status state machine:**

```
pending → done          (articles found + predictions extracted)
pending → no_predictions (articles found but gatekeeper/extractor returned 0 predictions)
pending → failed        (LLM returned invalid JSON after all retries)
pending → running       (currently being processed)
```

To re-process `no_predictions` cells: reset their status to `pending` and rerun the pipeline.

---

### `data/raw_ingest/{source_id}/{event_id}/article_NN.json`
Raw articles fetched by `gnews_ingest.py` before LLM processing.

```json
{
  "url": "https://www.timesofisrael.com/...",
  "headline": "Iran fires drones and missiles at Israel",
  "text": "Full article body...",
  "published_at": "2024-04-13",
  "author": "ToI Staff"
}
```

Articles under 500 chars are filtered (paywalled stubs). Liveblogs are skipped.

---

### `data/vault2/articles/{hash}.json`
Deduplicated article store. Key = SHA-256 of article text. Same article fetched from multiple events is stored once.

### `data/vault2/extractions/{hash}_{event_id}_v1.json`
LLM extraction result for a specific article × event pair. Cached — reused if article is seen again for the same event.

```json
{
  "extraction": {
    "predictions": [
      {
        "quote": "Iran will retaliate within days",
        "claim": "Iran will strike Israel within days",
        "stance": 0.85,
        "certainty": 0.9,
        "settled": null,
        "quantitative_estimate": null,
        "evidence_class": "reporting",
        "fact_signal": null,
        "event_actors": null,
        "event_target": null,
        "is_occurrence": null,
        "verified": null,
        "event_date": null,
        "event_date_reference": null
      }
    ]
  },
  "prompt_version": "v1",
  "run_date": "2026-03-22T08:14:00"
}
```

Legacy fields (`sentiment`, `specificity`, `hedge_ratio`, `conditionality`, `magnitude`,
`time_horizon`, `time_horizon_days`, `prediction_type`, `source_authority`) still appear
on entries produced before PR #102 and remain `Optional` on `PredictionExtraction` for
backward compatibility, but are no longer requested from the LLM. See
`docs/ARCHITECTURE.md` "Prediction (extracted by LLM)" for the current field set.

---

### `data/atlas/{event_id}/{source_id}/entry_{hash8}.json`
Rendered atlas entries — one file per article that produced predictions. These feed `render_atlas.py` to build `factum_atlas.html`.

---

## Pipeline flow

```
data/events/ + data/sources/
        ↓
  gnews_ingest.py          → data/raw_ingest/
        ↓
  orchestrator.py          → data/vault2/ + data/atlas/ + data/progress.json
        ↓
  render_atlas.py          → factum_atlas.html
```
