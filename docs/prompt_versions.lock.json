{
  "_comment": "Source of truth for api/tests/test_prompt_version_enforcement.py. Each entry pins BOTH halves of the text the model actually sees. `hash` is the SHA-256 (first 16 hex chars) of the hand-written prompt (forecaster.py's GATEKEEPER_PROMPT_HASH/EXTRACTOR_PROMPT_HASH). `schema_hash`/`schema_chars` cover the response model's JSON schema, which instructor's MD_JSON mode serialises into the prompt on every call — field descriptions, enum members and Pydantic docstrings included (retro#700); `schema_chars` is recorded separately from the hash so that growth shows up as a number in the diff, which is how the retro#681 regression should have been caught. Whenever an edit to either half changes model behavior materially: bump the matching *_PROMPT_VERSION constant in forecaster.py, add a row to docs/PROMPT_VERSIONS.md, and update this file to match. The test fails otherwise.",
  "_tail_blocks_comment": "`tail_blocks` (retro#731) is the THIRD half: instruction blocks appended to `prompt` AFTER PROMPT_SUFFIX.format() returns, so they sit outside the reconstructed *_PROMPT and outside the `hash` above. 6,576 chars of live prompt text that no lock could see. One entry per block on purpose — a single folded hash over the concatenation would report every change as 'the tail moved', which is not a review. Editing one of these IS a prompt edit (the retro#700 rule): bump the owning prompt's version, add a PROMPT_VERSIONS.md row, update the entry here. They are also CONDITIONALLY injected — a lexical pre-filter gates `extractor.conditional`, a host allowlist gates the two `short_form` blocks — so a regression in one lands on a subset of traffic and reads as drift rather than as a change.",
  "gatekeeper": {
    "version": "v1",
    "hash": "a09cdb5ecda0ce5e",
    "schema_hash": "1a225cf4e8e216a6",
    "schema_chars": 1178
  },
  "extractor": {
    "version": "v12",
    "hash": "659679b0199f7c17",
    "schema_hash": "44b9f1b31c65dce6",
    "schema_chars": 29799
  },
  "tail_blocks": {
    "extractor.conditional": {
      "hash": "1fb88674cd6d4b35",
      "chars": 4233
    },
    "extractor.short_form": {
      "hash": "992ad7ebf365b5fb",
      "chars": 604
    },
    "extractor.language_hint": {
      "hash": "ca09cdd7cc0eeda5",
      "chars": 189
    },
    "gatekeeper.short_form": {
      "hash": "af422c48d9b306f4",
      "chars": 1354
    },
    "gatekeeper.language_hint": {
      "hash": "04ce1a6034806976",
      "chars": 196
    }
  }
}
